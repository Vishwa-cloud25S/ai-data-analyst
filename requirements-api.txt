# API service only - deliberately excludes the UI stack (streamlit/plotly/pandas)
# so the container builds faster and fits comfortably in a 512 MB instance.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
duckdb==1.1.3
sqlglot==26.0.1
PyYAML==6.0.2
openai==1.59.6
python-dotenv==1.0.1
psycopg[binary]==3.2.3
