# Streamlit UI only - talks to the API over HTTP, so no database or LLM deps.
streamlit==1.41.1
plotly==5.24.1
pandas==2.2.3
requests==2.32.3
# layer_editor parses and rewrites the semantic layer client-side.
PyYAML==6.0.2
